name: Validate Site

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'bin/site/site-validate.sh'
      - 'bin/site/install-hugo.sh'
  pull_request:
    branches: [main]
    paths:
      - 'site/**'
      - 'bin/site/site-validate.sh'
      - 'bin/site/install-hugo.sh'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: ./bin/site/install-hugo.sh
      - run: ./bin/site/site-validate.sh
