# App Icon Consistency

## TODO

- Update the image at the top of the phone app to match the main app icon (add the white circle around it).
- Make the same change in the web app (`sweetspot/web`).
- In debug builds, make the circle red so it's immediately obvious you're running a debug build even with the app open.
