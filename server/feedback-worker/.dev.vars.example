# Copy to `.dev.vars` for local `wrangler dev` (gitignored). Production secrets are set with
# `wrangler secret put <NAME>` — see README.md. Never commit real values.
GITHUB_TOKEN=ghp_your_sweetspot_support_classic_public_repo_token
WEBHOOK_SECRET=generate_with_openssl_rand_hex_32
BREVO_API_KEY=xkeysib-your_brevo_transactional_api_key
